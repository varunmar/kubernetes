apiVersion: v1
kind: Pod
metadata:
  name: grpc-client
spec:
  containers:
  - name: grpc-client
    command: ["sh", "-c"]
    args: ["while true ; do sleep 5 ; done"]
    image: gcr.io/grpc-testing/grpc_interop_go1.8:v1.16.0
    imagePullPolicy: Always
